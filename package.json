{
  "name": "checklist-hub",
  "version": "1.0.0",
  "description": "ChecklistHub — a platform for creating, managing, and using checklist templates for various activities.",
  "keywords": [
    "checklist"
  ],
  "license": "ISC",
  "author": "Steven Uzunov",
  "type": "commonjs",
  "private": true,
  "workspaces": [
    "checklist-web",
    "checklist-mobile"
  ],
  "scripts": {
    "build": "npm --prefix ./checklist-web run build && npm --prefix ./checklist-mobile run build",
    "dev": "concurrently \"npm --prefix ./checklist-web run dev\" \"npm --prefix ./checklist-mobile run dev\""
  },
  "overrides": {
    "postcss": "^8.5.10"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "expo-router": "55.0.14",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  }
}
